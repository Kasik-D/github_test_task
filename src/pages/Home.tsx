import React from 'react';
import { Text } from 'react-native';

import { Layout } from '../components';

export const Home = () => {
  return (
    <Layout>
      <Text>Home</Text>
    </Layout>
  );
};
